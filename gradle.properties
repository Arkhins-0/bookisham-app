org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
org.gradle.parallel=false
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# --- Bookisham -------------------------------------------------------------
# Where the Bookisham server (the Next.js app) is deployed. No trailing slash.
# Override per machine in local.properties or with -Pbookisham.baseUrl=...
bookisham.baseUrl=https://bookisham.arkhins.com
# The public contact details shown to signed-out visitors: the same values
# as NEXT_PUBLIC_WHATSAPP and NEXT_PUBLIC_CONTACT_EMAIL on the server.
bookisham.whatsapp=917708204956
bookisham.contactEmail=krishnavijay.gkv@gmail.com
kotlin.daemon.jvmargs=-Xmx1024m
kotlin.compiler.execution.strategy=in-process
